# Shioaji API 模擬測試與啟用狀態驗證腳本規格

## 1. 專案目標

開發一個命令列工具，旨在簡化永豐金證券 Shioaji API 用戶的 API 啟用流程。此工具需提供兩個主要功能：

1.  **模擬測試 (Simulation Test)**：自動執行 API 啟用所需的模擬倉下單測試。
2.  **啟用驗證 (Activation Verification)**：在生產環境中檢查用戶的證券與期貨帳號是否已成功啟用。

## 2. 功能需求

### 2.1. 命令列介面 (CLI)

-   腳本應使用 `argparse` 模組來處理命令列參數。
-   提供兩個子命令：
    -   `test`：執行模擬測試流程。
    -   `verify`：執行帳號啟用狀態驗證流程。
-   應提供清晰的說明文字與使用範例。

### 2.2. 環境變數與使用者輸入

-   腳本應優先從環境變數讀取敏感資訊（如 API Key/Secret、憑證密碼等）。
-   若環境變數不存在，腳本應提示使用者手動輸入。
-   對於密碼等敏感資訊，輸入時應隱藏字元（不顯示在螢幕上）。

### 2.3. `test` 命令：模擬測試流程

1.  **連線模擬環境**：初始化 Shioaji API，並設定 `simulation=True`。
2.  **登入**：使用 API Key 與 Secret Key 登入。
3.  **帳號檢查**：自動抓取可用的證券與期貨帳號，若任一帳號不存在則提示錯誤並終止程式。
4.  **證券下單測試 (T02)**：
    -   商品：`2890` (永豐金)
    -   操作：買進 (Buy)
    -   價格：10 元 (LMT 限價單)
    -   數量：1 股
    -   委託類型：ROD
    -   下單後需檢查委託狀態，若失敗則提示錯誤並終止。
5.  **頻率控制**：為符合 API 1秒1次的下單頻率限制，在證券下單後需暫停 1 秒。
6.  **期貨下單測試 (T03)**：
    -   商品：台指期近月合約 (TXF)，需排除轉倉合約 (TXFR1, TXFR2)。
    -   操作：買進 (Buy)
    -   價格：18000 點 (LMT 限價單)
    -   數量：1 口
    -   委託類型：ROD
    -   倉別：自動 (Auto)
    -   下單後需檢查委託狀態，若失敗則提示錯誤並終止。
7.  **完成**：所有下單成功後，顯示成功訊息並自動登出。

### 2.4. `verify` 命令：啟用狀態驗證流程

1.  **連線正式環境**：初始化 Shioaji API，並設定 `simulation=False`。
2.  **憑證啟用**：使用使用者提供的憑證路徑、密碼與身分證字號來啟用 CA 憑證。
3.  **登入**：使用 API Key 與 Secret Key 登入正式環境。
4.  **狀態檢查**：遍歷所有帳號，檢查證券與期貨帳號的 `signed` 屬性。
5.  **結果顯示**：清晰地條列出各帳號的啟用狀態（例如：`🟢 已啟用` 或 `🟡 未啟用`）。
6.  **完成**：顯示總結訊息後自動登出。